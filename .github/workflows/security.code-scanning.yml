name: CodeQL

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  codeql-python:
    uses: chargehound/security-workflows/.github/workflows/codeql-python.yml@main